<div class="container">
 <h2>商品詳細</h2>
  <div class="row">
   <div class="col-md-9">

     <div class="form-group">
       <%= image_tag @item.get_image(300,250) %>

       <p></p>
        <%= @item.name %>
        <%= @item.introduction %>
        <%= @item.name %>
        #ジャンル追加